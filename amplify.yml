version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd O-Zone-UI
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: O-Zone-UI/dist
    files:
      - '**/*'
  cache:
    paths:
      - O-Zone-UI/node_modules/**/*
